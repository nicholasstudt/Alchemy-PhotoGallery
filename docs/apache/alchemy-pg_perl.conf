<Perl>
	#!/usr/bin/perl

	## Dependencies 
	use Apache2::Request;
	use File::Find::Rule;
	use Image::Magick;
	use Image::ExifTool;
	use KrKit::Control;
	use KrKit::Handler;
	use KrKit::HTML;
	use KrKit::Validate;
	use POSIX;

	## PhotoGallery 
	use Alchemy::PhotoGallery;
	use Alchemy::PhotoGallery::Admin;
	use Alchemy::PhotoGallery::Viewer;
</Perl>

